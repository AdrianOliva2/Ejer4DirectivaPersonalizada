import { EnlaceOverDirective } from './enlace-over.directive';

describe('EnlaceOverDirective', () => {
  it('should create an instance', () => {
    const directive = new EnlaceOverDirective();
    expect(directive).toBeTruthy();
  });
});
